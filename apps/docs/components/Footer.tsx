export function Footer() {
  return (
    <footer className="shell site-footer">
      LensUI is open-source and host-agnostic. Production hosts keep provider keys, billing, and tool permissions server-side.
    </footer>
  );
}
