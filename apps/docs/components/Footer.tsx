export function Footer() {
  return (
    <footer className="shell site-footer">
      LensUI is open-source and host-agnostic. Agents stream lightcode; hosts keep provider keys, billing, memory, and permissions server-side.
    </footer>
  );
}
